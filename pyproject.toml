[project]
name = "webnovel-scraper"
version = "0.0.1"
dependencies = ["pypub3"]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["."]